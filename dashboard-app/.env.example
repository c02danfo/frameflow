# Dashboard app env (EXAMPLE)
# Copy to `dashboard-app/.env` if you run this app standalone.
# When running via docker-compose.production.yml, env is provided by Compose.

PORT=3010
NODE_ENV=development

AUTH_DB_HOST=auth-db
AUTH_DB_PORT=5432
AUTH_DB_USER=auth_user
AUTH_DB_PASSWORD=change_me
AUTH_DB_NAME=frameflow_auth

AUTH_SERVICE_URL=http://auth-service:3005

SESSION_SECRET=CHANGE_ME_GENERATE_A_LONG_RANDOM_SECRET
JWT_SECRET=CHANGE_ME_GENERATE_A_LONG_RANDOM_SECRET

DOMAIN=frameflow.test
