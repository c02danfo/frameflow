<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FrameFlow - Login</title>
    <link rel="stylesheet" href="/design-system/css/theme.css">
    <link rel="stylesheet" href="/design-system/css/components.css">
    <style>
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }
        .login-container {
            width: 100%;
            max-width: 400px;
        }
        .login-card {
            margin: 0 var(--spacing-lg);
        }
        .login-header {
            text-align: center;
            margin-bottom: var(--spacing-2xl);
        }
        .login-header h1 {
            margin: 0;
            color: var(--gray-900);
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="card login-card">
            <div class="login-header">
                <h1>FrameFlow</h1>
                <p style="color: var(--gray-600); margin-top: var(--spacing-sm);">Inventory Management</p>
            </div>
            
            <% if (error) { %>
                <div class="alert alert-danger">
                    <%= error %>
                </div>
            <% } %>
            
            <form method="POST" action="/auth/login">
                <div class="form-group">
                    <label for="email">E-post</label>
                    <input 
                        type="email" 
                        id="email"
                        name="email" 
                        required 
                        placeholder="admin@demo.local"
                    >
                </div>
                
                <div class="form-group">
                    <label for="password">Lösenord</label>
                    <input 
                        type="password" 
                        id="password"
                        name="password" 
                        required 
                        placeholder="••••••••"
                    >
                </div>
                
                <button type="submit" class="btn btn-primary" style="width: 100%;">
                    Logga in
                </button>
            </form>
            
            <p class="text-xs text-gray-500 text-center mt-4">
                Standardinloggning: admin@demo.local / admin123
            </p>
        </div>
    </div>
</body>
</html>
