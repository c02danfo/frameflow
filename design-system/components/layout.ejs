<%- /* 
Layout Component
Usage: res.render('layout', { body: htmlContent, user: userData, domain: 'frameflowapp.com' })
*/ -%>

<!DOCTYPE html>
<html lang="sv">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'FrameFlow' %></title>
    
    <!-- Tailwind CSS CDN -->
    <script src="https://cdn.tailwindcss.com?plugins=forms,typography"></script>
    
    <script>
        // Tailwind config
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#0f172a',
                        accent: '#0ea5e9',
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gray-50 min-h-screen">
    <%- include('./navbar.ejs') %>
    
    <main>
        <%- body %>
    </main>
    
</body>
</html>
